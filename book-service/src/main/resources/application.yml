spring:
  application:
    name: book-service
  datasource:
    username: postgres
    password: database09
    url: jdbc:postgresql://localhost:5432/book_db
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080