spring:
  application:
    name: author-service
  datasource:
    username: postgres
    password: database09
    url: jdbc:postgresql://localhost:5432/author_db
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8081